# API VolleyFlow

Todos los endpoints usan prefijo `/api`. La API expone autenticación, jugadores, canchas y jornadas según `prompt-codex.md`. Swagger está disponible en desarrollo o con `SWAGGER_ENABLED=true`.
